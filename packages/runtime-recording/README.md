# @origin/runtime-recording

Platform-neutral recording records, state machine, retention, JSONL line formats, and ports.

Desktop owns the Electron offscreen capture engine. Node owns the ffmpeg encoder. This package owns the record contract that Evaluation consumes as evidence.
