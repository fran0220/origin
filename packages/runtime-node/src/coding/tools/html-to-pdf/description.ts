export const HTML_TO_PDF_TOOL_DESCRIPTION = `Convert an HTML file to a PDF using Origin Desktop's bundled Electron Chromium renderer.

This tool calls the Origin Desktop executable in command-line PDF mode. Origin Desktop must be installed, or VETTA_DESKTOP_EXE must point to the desktop executable.

Input: HTML file path and required output PDF path
Output: PDF file path`;
